/**
 * basic identity cipher
 * @param x string to be encrypted
 */
export function cipher(x: string): string {
  return x;
}

/**
 * add exclamation mark to string
 * @param s string to add exclamation mark to
 */
export function addExclamation(s: string): string {
  return `${s}!`;
}
